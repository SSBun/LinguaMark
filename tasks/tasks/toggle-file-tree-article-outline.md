# 在文件树与文章目录之间切换

Status: Completed (2026-09-02 15:33)
Kind: Task

## Scope

- 包含：让现有侧栏控制在目录文件树与当前文章目录之间切换，并随当前内容更新可用视图。
- 不包含：跨页面持久化侧栏视图选择或重做其他工具栏控件。

## Target
- [x] T1: 用户可通过现有文件按钮在文件树与当前文章目录之间切换，并能正常使用两种视图

## Plan

1. 梳理侧栏显隐、目录恢复和文章切换的共享状态。
2. 复用现有侧栏容器增加视图选择，并同步当前文件树与文章目录。
3. 验证两种视图往返切换、内容更新、降级行为、类型检查与构建。

## Result

- T1: Chromium 冒烟检查确认顶部原生选择控件可在文件树、当前文章目录和隐藏侧栏间往返切换，打开新文章后目录实时更新，图片预览自动降级为文件树。
- Review gate: Skipped — 用户未请求独立 Reviewer；已自查异步目录恢复、无标题或图片降级、隐藏状态、窄屏抽屉和最小实现。

## Verification

- Passed: npm run check、npm run build、git diff --check、Context/Lessons 校验及当前构建的 Chromium 交互冒烟检查均通过；按项目规则未运行测试套件。
