# 为文件树增加文件夹折叠工具栏

Status: Completed (2026-08-31 20:06)
Kind: Task

## Scope

- 包含：已授权目录文件树的全量展开、全量折叠控制，以及文件树加载时的初始折叠状态。
- 不包含：侧栏整体显隐、文件打开行为或文件夹展开状态持久化。

## Target
- [x] T1: 文件树顶部显示工具栏，可统一展开或折叠文件夹。
- [x] T2: 每次加载文件树时，所有文件夹默认处于折叠状态。

## Plan

1. 在文件树顶部加入可访问的全量展开与折叠入口。
2. 将文件夹初始状态改为折叠，并保持既有文件项交互不变。
3. 验证默认状态、批量切换、空目录表现和构建检查。

## Result

- T1: 构建后的 Viewer 经 Headless Chromium 冒烟检查，显示“全部展开”和“全部折叠”按钮，点击后两层文件夹分别全部展开、全部折叠。
- T2: 同一浏览器冒烟检查确认文件树首次渲染后两层 details 节点均为关闭状态。
- Review gate: Skipped — 用户未请求独立 Reviewer；已完成本地代码与渲染结果自审。

## Verification

- Passed: npm run check 与 npm run build 均退出 0；Headless Chromium 冒烟检查通过默认折叠、全部展开和全部折叠。
