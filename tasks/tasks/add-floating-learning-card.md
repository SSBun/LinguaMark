# 增加悬浮学习卡片

Status: Completed (2026-08-31 14:29)
Kind: Task

## Target
- [x] T1: 页面提供可拖动、可关闭的悬浮入口；停留 2 秒会显示学习卡片。
- [x] T2: 英文学习卡片会列出文章中的特殊词汇、经典短语和句式，并允许用户逐项收藏。
- [x] T3: 收藏内容在本地持久化并去重；已收藏内容再次被列举时会自动显示收藏状态。
- [x] T4: 悬浮窗整体尺寸比当前版本更小。
- [x] T5: 点击圆球只会向上展开菜单，不会自动解析；仅点击菜单中的解析操作才会发起解析。

## Plan

1. 缩小悬浮窗整体尺寸。
2. 将圆球点击改为向上展开菜单，并把解析触发移入菜单操作。
3. 完成类型检查、构建检查与改动审查。

## Result

- T2: 两个解析 Adapter 已产出并校验词汇、短语、句式条目，Content script 增量渲染逐项收藏列表；类型检查与构建通过。
- T3: 收藏使用 learningFavorites 本地存储，并以类型加规范化文本键去重和恢复 aria-pressed 已收藏状态；静态边界检查通过。
- T4: 最终构建与浏览器检查显示悬浮圆球为 44×44，学习卡片最大尺寸由 360×540 缩至 300×420。
- T5: 浏览器交互检查显示圆球点击仅在上方展开菜单且分析消息数为 0；点击“解析当前文章”后才发送一次 ANALYZE_CURRENT_TAB 并打开卡片。
- T1: 浏览器交互检查显示悬浮入口可拖动至视口边界、可关闭，指针停留 2 秒仍会打开学习卡片。
- Review gate: Skipped — 用户未要求独立 Reviewer 审批。

## Verification

- Passed: npm run check、npm run build、git diff --check、浏览器 DOM 交互检查与 Context validate 均通过；按当前会话规则未运行测试套件。
