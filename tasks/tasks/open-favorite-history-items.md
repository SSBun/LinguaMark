# 从收藏与历史列表打开项目

Status: Completed (2026-09-02 11:47)
Kind: Task

## Scope

- 包含：打开当前仍可访问的收藏或历史项目；项目不可访问时提示重新选择或授权。
- 不包含：为每个项目持久化文件系统句柄。

## Target
- [x] T1: 点击收藏列表或历史列表中当前可访问的文件或文件夹项目，会打开对应项目。
- [x] T2: 点击当前不可访问的项目时，会提示用户重新选择或授权。
- [x] T3: 点击当前不可访问的目录项目时，会展示可操作的授权引导；用户完成重新选择或授权后可打开原目标，取消时保持现状，现有可访问项目的打开行为不变。

## Plan

1. 确认无权限目录条目的失败路径与现有授权流程。
2. 将被动提示改为可操作的授权确认，并在授权完成后继续打开原目标。
3. 运行类型检查、构建和静态行为核验。

## Result

- T1: 检查 src/markdown.ts 与构建产物：收藏和历史条目渲染为原生按钮；当前授权目录项目复用目录读取流程，目录项显示文件树，绝对本地路径跳转对应 file:// 页面；npm run check 与 npm run build 通过。
- T2: 检查 src/markdown.ts 与静态验证输出：缺少目录上下文、独立文件选择上下文或 file:// 权限时均关闭弹窗并显示重新选择或授权提示；8/8 静态检查通过。
- T3: Chromium 核验：无权限目录或目录内文件会显示含重新授权／重新选择说明的原生确认；取消保持当前内容，确认会打开对应 Viewer 的授权窗口，正确授权通知后自动打开原文件，已有权限项目仍直接打开。
- Review gate: Skipped — 用户未要求独立对抗性审查。

## Verification

- Passed: npm run check、npm run build、7/7 静态检查、git diff --check、Context／Lessons 校验及 Chromium 四条授权交互核验均通过；按项目规则未运行测试套件。
