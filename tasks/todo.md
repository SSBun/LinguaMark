# 已完成任务：提交并推送全部本地改动

- [x] 审核全部已跟踪与未跟踪改动、远端关系及敏感信息风险。
- [x] 运行类型检查、构建与 Git 差异检查。
- [x] 将全部本地改动提交并推送到当前分支的远端上游。
- [x] 核验工作树清洁且远端包含本地最新提交。

## Review

- 变更：全部应用与任务改动已提交到 `main` 并推送；本机专用 `.mcp.json` 已移出 Git 跟踪并加入忽略规则。
- 验证：`npm run check`、`npm run build`、3 个测试脚本语法检查、`git diff --check`、Context／Lessons／Task 校验、高置信度凭据扫描以及两轮强制只读审查通过；第二轮无 finding，`TR-1` 已解决。
- 未解决风险：按当前请求及项目规则未运行测试套件；该限制不阻塞 Git 提交与推送目标。

---

# 已完成任务：为字词标签预留独立侧栏区域

- [x] 保留低干扰标签及中英“字词”统计规则。
- [x] 为标签预留独立空间，不覆盖文章目录或文件树。
- [x] 保持目录和文件树可完整滚动，并兼容窄屏侧栏。
- [x] 运行类型检查、构建和桌面／窄屏浏览器冒烟核验。

## Review

- 变更：桌面侧栏底部预留 32px 状态区域，使文章目录和文件树在字词标签上方结束；窄屏继续使用高层级抽屉，不被标签覆盖。
- 验证：`npm run check`、`npm run build`、`node --check tests/file-picker.mjs`、`git diff --check`、Context／Lessons 校验通过；1200×900 下目录与标签间隔 17.6px、重叠为 0，80 项长目录可滚动至最后一项，375×812 抽屉不受标签遮挡；强制只读审查无 finding。
- 未解决风险：按项目规则未运行测试套件；文件树长列表和极低桌面视口主要依靠共享布局规则的静态核验。

---

# 已完成任务：复用已打开文件的 Chrome 标签页

- [x] 确认本地文件打开路径与重复标签页边界。
- [x] 已打开同一文件时定位并刷新原页面，不保留重复页面。
- [x] 保持首次打开文件的现有行为。
- [x] 运行类型检查、构建和浏览器冒烟核验。

## Review

- 变更：直接打开同一本地 Markdown 时复用并刷新既有 `file://` 标签页；独立 Reader 以原生文件句柄识别同一文件，刷新既有 Reader，并关闭新建的空白重复页。
- 验证：`npm run check`、`npm run build`、`node --check tests/file-picker.mjs`、`git diff --check` 通过；隔离 Chrome 验证首次打开保留新页，同窗口和跨窗口重复打开均只保留并激活一个既有页且发生重载；独立 Reader 使用同一 FileSystemFileHandle 重复选择后关闭空白页、激活旧页并读取更新后的内容；Context 与 Lessons 校验通过。
- 未解决风险：按项目规则未运行测试套件；系统原生文件面板调用参数未改变，因此未重复拉起面板。

---

# 已完成任务：修复 Mermaid 图表渲染不完整

- [x] 复现首张截图中的图表并定位连线标签裁切根因。
- [x] 确认最新截图缺失的是节点文字的最后一个字符。
- [x] 记录节点标签视口与实际文字尺寸并定位共用根因。
- [x] 最小化修复节点与连线标签的内容裁切。
- [x] 运行类型检查、构建与浏览器冒烟检查。

## Review

- 变更：Mermaid HTML 标签不再继承正文段落的 `0.01em` 字距，文字实际宽度与 Mermaid 预先计算的 `foreignObject` 宽度重新一致；保留连线标签装饰框的溢出与居中修复。
- 验证：`npm run check`、`npm run build`、`git diff --check` 均通过；截图对应的 6 个 DIP 节点文字左右溢出均为 0，整篇文档 129 个 Mermaid 节点标签无裁切，两个连线标签完整，Mermaid gallery 的 14 个面板无渲染失败。
- 未解决风险：按项目规则未运行测试套件。

---

# 已完成任务：在文件树与文章目录之间切换

- [x] 梳理侧栏显隐、目录恢复和文章切换状态。
- [x] 将现有侧栏按钮优化为文件树／文章目录视图选择。
- [x] 保持两种视图随当前文章和目录状态同步。
- [x] 运行类型检查、构建与交互差异审查。

## Review

- 变更：顶部侧栏按钮改为原生选择控件，可选择文件树、文章目录或隐藏侧栏；两种视图保留各自状态，并在打开新 Markdown、图片或恢复目录后同步可用性与当前内容。
- 验证：`npm run check`、`npm run build`、`git diff --check`、Context／Lessons 校验及 Chromium 桌面切换、目录实时更新、隐藏、窄屏抽屉和图片降级冒烟检查均通过。
- 未解决风险：按项目规则未运行测试套件。

---

# 已完成任务：修复不可见的 `file://` 目录选择器

- [x] 根据 `File picker already active` 证据确认首个系统 picker 已激活但不可见。
- [x] 将 `file://` 目录入口改为标准 directory file input，不重复调用隐藏的 picker。
- [x] 从 FileList 建立当前标签页文件树并保持 Markdown／图片读取。
- [x] 保持 standalone Viewer 的系统 picker 和跨重载授权。
- [x] 增加回归检查并运行完整测试套件。

## Review

- 变更：`file://` 使用隐藏的 `webkitdirectory` file input 打开可见系统选择器，并从 `webkitRelativePath` 构建受限文件树和内存文件映射；standalone 继续使用 File System Access API；旧 LinguaMark 中间页保持删除。
- 验证：CGWindow 核验点击后新增 on-screen Chrome 窗口；FileChooser 使用临时目录验证选择、读取和取消；`npm run verify` 全部通过，包括 17 个单元测试、文件／目录 picker 回归和 Chrome E2E。
- 未解决风险：标准 directory input 无法表示空目录，选择空目录等同无文件；按方案 A，`file://` 页面重载后仍需重新选择目录。

---

# 已完成任务：直接打开系统目录选择器

- [x] 验证 `file://` 内容脚本可直接调用目录选择器，并确认页面／扩展 IndexedDB 来源边界。
- [x] 单次点击直接显示系统目录选择器，不再打开 LinguaMark 中间页。
- [x] 保持目录文件树、文件读取、取消、standalone 跨重载恢复及 `file://` 会话隔离。
- [x] 删除不再使用的中间页与消息路径。
- [x] 增加目录 picker 回归并运行完整测试套件。

## Review

- 变更：文件页与 standalone Viewer 的目录按钮均在当前点击内直接调用系统选择器；standalone 继续按标签页持久化，`file://` 新句柄仅保留在内容脚本内存；删除原目录授权页面、样式、脚本及 Background 打开窗口消息。
- 验证：`file://` 原生目录面板约 117.9ms 出现且没有中间页，watchdog 安全关闭；`npm run verify` 全部通过，包括 17 个单元测试、文件／目录 picker 回归和 Chrome E2E。
- 未解决风险：按用户选择的方案 A，`file://` 页面新选择的目录在重载后需要重新选择；这是为避免把目录句柄暴露给页面来源而保留的安全边界。

---

# 已完成任务：真实复现并测试“打开文件”卡死

- [x] 安装并接入 Chrome DevTools MCP，加入隐私安全的 picker 日志。
- [x] 用隔离 Chrome 与 watchdog 安全触发并关闭原生文件选择器，不发送按键或选择文件。
- [x] 定位卡死层级并修复最小调用路径。
- [x] 增加聚焦回归检查，覆盖选择、取消和页面可用性。
- [x] 运行类型检查、构建、聚焦测试和项目测试套件。

## Review

- 变更：“打开文件”使用 `showOpenFilePicker({ startIn: "downloads" })` 绕过历史云盘位置，等待期间不禁用按钮并保留隐私安全日志；新增 picker 浏览器回归检查；Chrome E2E 适配当前 Pi 消息内容结构与 stale 响应行为。
- 验证：Chrome for Testing／Stable Chrome 的原生面板分别在 requested 后约 253.3ms／163.5ms 启动并由 watchdog 安全关闭；`npm run verify` 全部通过，包括 17 个单元测试、picker 回归和 Chrome E2E。
- 未解决风险：隔离环境未复现持续卡死，证据将剩余问题定位到用户环境中的 macOS 文件面板／File Provider；CDP `Escape` 无法安全取消原生 chooser，后续不得自动化该动作。

---

# 已完成任务：让“打开文件”直接显示系统选择器

- [x] 确认加载光标来自等待 `showOpenFilePicker()` 时禁用按钮。
- [x] 改用浏览器原生文件输入直接打开系统选择器，不显示持续加载状态。
- [x] 保持 Markdown 类型限制、选择后打开及取消行为。
- [x] 运行类型检查、构建、Chromium 交互核验与差异审查。

## Review

- 变更：“打开文件”改用隐藏的原生 file input 同步触发系统选择器；等待期间不禁用按钮，不再显示加载光标；删除无调用方的 File System Access 文件 picker 类型声明。
- 验证：`npm run check`、`npm run build`、6/6 静态检查、`git diff --check`、Context／Lessons 校验、Chromium 6/6 系统 filechooser／按钮状态／Markdown 打开检查及取消保持检查均通过；filechooser 事件约 36.7ms 出现。
- 未解决风险：系统文件面板本身若因 macOS 或云盘服务异常仍可能延迟，但页面不会再显示假死加载状态。按项目规则未运行测试套件。

---

# 已完成任务：为无权限目录提供授权引导

- [x] 确认收藏／历史条目与目录授权窗口的调用链。
- [x] 点击无权限目录或目录内文件时展示可操作的授权确认。
- [x] 授权成功后继续打开原目标，取消或选错目录时安全降级。
- [x] 运行类型检查、构建、静态行为核验与差异审查。

## Review

- 变更：点击缺少目录权限的收藏或历史项目时显示原生确认弹窗；确认后进入既有目录授权窗口，并在正确授权后自动继续打开原目标。
- 验证：`npm run check`、`npm run build`、7/7 静态检查、`git diff --check`、Context／Lessons 校验，以及 Chromium 确认取消、授权窗口打开、授权完成后恢复原文件和已有权限直接打开核验均通过。
- 未解决风险：浏览器只暴露目录名称；两个同名目录仍无法仅凭名称区分。按项目规则未运行测试套件。

---

# 已完成任务：自动定位目录中的当前文件

- [x] 确认打开文件与目录恢复共用的当前文件选中路径。
- [x] 让当前文件被选中时自动展开父目录、滚动到可视区域并获得焦点。
- [x] 保持手动定位与当前文件不在目录树时的降级行为。
- [x] 运行类型检查、构建、静态行为核验与差异审查。

## Review

- 变更：打开目录文件或异步恢复目录树时，复用当前文件选中路径自动展开父目录、居中滚动并聚焦；手动定位按钮复用同一行为。
- 验证：`npm run check`、`npm run build`、`git diff --check`、自动／手动／树外降级与异步恢复 Chromium 冒烟检查、Context 和 Lessons 校验均通过。
- 未解决风险：按项目规则未运行测试套件。

---

# 已完成任务：悬浮窗点击后展示纵向胶囊菜单

- [x] 将现有横向菜单改为围绕悬浮球展开的纵向长胶囊。
- [x] 保留“解析当前文章”的操作与无障碍语义。
- [x] 保持拖动、收起及视口边界行为。
- [x] 运行类型检查、构建、静态交互核验与差异审查。

## Review

- 变更：点击 LM 后展开 52×106px 的纵向胶囊；“解析当前文章”以文字标签和“析”图标呈现，收起后仍为 44×44px 圆球。
- 验证：`npm run check`、`npm run build`、`git diff --check`、5/5 静态检查、Chromium 扩展点击／边界／解析入口冒烟检查及 Context 校验均通过。
- 未解决风险：当前只保留一个既有菜单项；按项目规则未运行测试套件。

---

# 已完成任务：增加重载滚动动画开关

- [x] 在 Markdown Viewer 工具栏增加默认关闭且可持久化的动画开关。
- [x] 仅在开关开启时启用重载恢复位置的平滑滚动。
- [x] 保留无动画的位置恢复及系统“减少动态效果”行为。
- [x] 运行类型检查、构建与静态差异核验。

## Review

- 变更：阅读器工具栏新增无障碍重载动画开关；状态保存在当前标签页的会话存储中并跨重载保留；平滑滚动只在开启时应用，系统“减少动态效果”仍强制关闭动画。
- 验证：`npm run check`、`npm run build`、`git diff --check`、静态状态检查、`file://` 会话存储检查、独立 Viewer 开关／重载／Content script 初始化冒烟检查、关闭与开启状态下 3200／4700px 滚动位置恢复检查、系统“减少动态效果”检查、375px 工具栏检查及 Context 校验均通过。
- 未解决风险：开关按当前标签页保存，不跨新标签页同步；极窄 320px 窗口的既有工具栏仍可能横向溢出。

---

# 已完成任务：更新 README 的特性、安装、使用与徽章

- [x] 核对当前功能、兼容性、发布包和构建命令。
- [x] 将 README 重组为面向使用者的功能与操作说明，并添加项目徽章。
- [x] 核验文档格式、链接、安装命令和构建结果。

## Review

- 变更：README 改为面向使用者的功能、安装、配置、网页分析、本地 Markdown 阅读、隐私与限制说明，并添加 Release、下载量、Chrome、Manifest V3 和 TypeScript 徽章。
- 验证：`npm run check`、`npm run build`、`git diff --check`、README 章节／本地链接／命令静态核验、5 个徽章与 Release 外部链接 HTTP 200，以及发布包根目录包含 `manifest.json` 均通过。
- 未解决风险：按项目规则未运行测试套件，也未在真实 Chrome 中手动走完安装与使用流程。

---

# 当前任务：提交全部本地改动并发布 GitHub Release 0.1.0

- [x] 审查全部待提交文件、版本元数据与敏感信息。
- [x] 运行类型检查和构建，生成并核验 0.1.0 扩展压缩包。
- [x] 提交全部本地改动。
- [x] 创建公开仓库 `ssbun/LinguaMark` 并推送提交。
- [x] 发布并核验 GitHub Release 0.1.0。

## Review

- 变更：提交全部既有本地改动，创建公开仓库 `SSBun/LinguaMark`，并发布带可安装扩展压缩包的 `v0.1.0` GitHub Release。
- 验证：`npm run check`、`npm run build`、`git diff --check`、ZIP 完整性与必需文件检查、远程 main/tag 提交一致性及下载资产 SHA-256 校验均通过。
- 未解决风险：按项目规则未运行测试套件；本次发布不包含 Chrome Web Store 发布。

---

# 已完成任务：让收藏列表 cell 紧凑纵向排列

- [x] 保留“全部收藏”和“最近浏览”两个列表。
- [x] 让每个列表的 cell 从顶部按内容高度依次排列。
- [x] 保持统一的 cell 内边距和列表间距。
- [x] 验证稀疏列表、滚动、尺寸调整和窄屏布局。

## Review

- 变更：列表隐式行改为内容高度并顶端对齐，保留两个列表、10px cell 内边距和 8px 列表间距，不再按剩余高度拉伸稀疏 cell。
- 验证：`npm run check`、`npm run build`、6/6 紧凑列表检查及 Chrome 双列表、固定间距、长列表滚动、尺寸调整和窄屏冒烟检查通过。
- 未解决风险：按项目规则未运行测试套件。

---

# 已完成任务：放大并支持调整收藏列表弹窗尺寸

- [x] 增大桌面端默认宽度和高度。
- [x] 使用原生拖动调整，并限制最小尺寸和视口上限。
- [x] 让内部双列列表填满调整后的空间。
- [x] 保持窄屏单列与关闭按钮可用。
- [x] 运行类型检查、构建和浏览器尺寸冒烟检查。

## Review

- 变更：桌面弹窗默认 1080×780px，支持原生双向调整并受最小尺寸和视口限制；内部列表填满高度；700px 以下改为视口内单列且禁用调整。
- 验证：`npm run check`、`npm run build`、6/6 尺寸源码检查及 Chrome 默认尺寸、拖动调整、视口边界、窄屏和关闭冒烟检查通过。
- 未解决风险：调整后的尺寸仅保留在当前页面；按项目规则未运行测试套件。

---

# 已完成任务：同步导航栏与右键菜单收藏状态

- [x] 统一当前文件绝对路径与目录树相对路径的收藏身份。
- [x] 两个入口使用同一状态和增删逻辑。
- [x] 目录树异步恢复后重新计算收藏状态并隐藏旧重复项。
- [x] 验证两个方向的收藏切换、类型与构建结果。

## Review

- 变更：当前文件的绝对路径和目录树根相对路径共享收藏身份；导航栏、右键菜单与收藏列表统一读取，取消收藏会移除全部别名。
- 验证：`npm run check`、`npm run build`、7/7 同步检查及 Chrome 双向切换、旧别名、异步恢复和树外降级冒烟检查通过。
- 未解决风险：同名根目录且相对路径相同仍无法仅凭浏览器信息区分；按项目规则未运行测试套件。

---

# 已完成任务：扩展路径树右键菜单

- [x] 移除 “Open in File Picker”。
- [x] 为所有项目增加 “Copy Relative Path” 与 “Copy Name”。
- [x] 为文件增加收藏切换。
- [x] 为文件夹增加子树展开与折叠。
- [x] 运行类型检查、构建和菜单行为检查。

## Review

- 变更：所有项目可复制相对路径和名称；文件可切换收藏；文件夹可递归展开或折叠子树；目录选择操作已移除。
- 验证：`npm run check`、`npm run build`、7/7 源码检查及 Chrome 菜单、剪贴板、收藏增删和文件夹子树冒烟检查通过。
- 未解决风险：未在真实扩展目录中手动操作；按项目规则未运行测试套件。

---

# 已完成任务：修复当前文件定位按钮错误禁用

- [x] 将目录文件树控制区移动到列表面板上方。
- [x] 将全部展开／折叠合并为一个切换按钮。
- [x] 增加当前文件定位按钮，并展开父目录、选中且滚动到当前文件。
- [x] 文件树初始化时恢复当前目录文件的选中状态。
- [x] 运行类型检查、构建与聚焦浏览器冒烟检查。

## Review

- 变更：目录树恢复后会把已打开文件的绝对路径映射为树内相对路径，立即恢复选中状态并启用定位按钮。
- 验证：`npm run check`、`npm run build` 与 Chrome 初始化路径冒烟检查通过；已打开的两层嵌套文件保持默认折叠但定位按钮可用，点击后父目录展开并聚焦文件项。
- 未解决风险：浏览器只暴露授权目录名称，两个同名目录且相对路径相同时无法仅凭路径区分；该情况只影响列表定位，不影响文件读取。按项目规则未运行测试套件。

---

# 当前任务：让 Markdown 阅读器左侧目录可调整宽度

- [x] 为文档目录与目录文件树增加共用拖拽边界。
- [x] 支持键盘调整并保持侧栏显隐与窄屏抽屉行为。
- [x] 运行类型检查、构建和静态行为检查。

## Review

- 变更：桌面端左侧栏新增共用分隔器，支持拖动、方向键及 Home／End 调整 200px 至半视口宽度；窄屏继续使用固定抽屉。
- 验证：`npm run check`、`npm run build`、`git diff --check`、6/6 源码与构建产物静态检查、Chromium 拖动／键盘／窄屏／无目录烟雾检查和 Context 校验均通过。
- 未解决风险：未在真实扩展授权目录中手动拖动文件树；按项目规则未运行测试套件。

---

# 已完成任务：修复路径树右键菜单立即关闭

- [x] 将右键菜单从自动 light-dismiss 改为受控关闭。
- [x] 点击菜单外部或按 `Escape` 时关闭菜单并恢复焦点。
- [x] 保持两个菜单操作和路径语义不变。
- [x] 运行类型检查、构建与生命周期静态检查。

## Review

- 变更：右键菜单改用 manual Popover，显式处理菜单替换、点击外部、`Escape`、操作完成、焦点恢复和监听器清理。
- 验证：`npm run check`、`npm run build`、7/7 生命周期静态检查和 Chrome 浏览器冒烟检查通过。
- 未解决风险：未在真实扩展目录中手动操作；按项目规则未运行测试套件。

---

# 已完成任务：隔离 Reader 页面打开状态

- [x] Popup 每次打开新的空白 Reader 页面。
- [x] 每个 Reader 页面独立保存和读取自己的目录授权；单文件内容保持页面内隔离。
- [x] 保留收藏、最近浏览及直接 `file://` Markdown 的既有行为。
- [x] 更新 README 与项目上下文中的 Reader 行为说明。
- [x] 运行类型检查、构建检查和差异审查。

## Review

- 变更：Popup 不再复用 Reader 标签页；独立 Reader 的目录句柄按标签页隔离并在关闭时清理，直接 `file://` Markdown 继续使用既有默认句柄。
- 验证：`npm run check`、`npm run build`、源代码不变量检查、`git diff --check` 与 Context 校验均通过。
- 未解决风险：按项目规则未运行自动化测试，也未在真实 Chrome 中手动操作目录选择器。
